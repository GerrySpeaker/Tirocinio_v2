import { Router } from "./router.js";

document.addEventListener("DOMContentLoaded", () => {
  new Router("#content");

  // Aggiungi dopo: new Router("#content");
document.getElementById('inizia')?.addEventListener('click', () => {
  alert('Lezione iniziata! (Domani aggiungeremo le domande da MongoDB)');
  documentクエ.querySelector('.progress-bar').style.width = '8%';  // esempio progresso
});
});
